server:
  port: 8080
spring:
  application:
    name: chat-service

llm:
  provider: ollama
  base-url: http://localhost:11434
  model: qwen2.5:14b
  timeout-seconds: 120

management:
  endpoints:
    web:
      exposure:
        include: health, info